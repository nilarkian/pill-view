{
  "name": "view",
  "version": "0.2.0",
  "description": "Base view for Obsidian",
  "type": "module",
  "main": "./main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "obsidian": "^1.4.11"
  }
}
